{
  "name": "project-root",
  "version": "1.0.0",
  "scripts": {
    "build-client": "npm --prefix client run build",
    "deploy": "npm run build-client && xcopy /E /I client\\build server\\public"
  },
  "dependencies": {
    "react-icons": "^5.3.0"
  }
}
